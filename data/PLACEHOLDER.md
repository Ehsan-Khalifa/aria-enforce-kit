# Optional: event data for the Congestion-Alert tab

Copy the Astram event CSV here and rename it `astram_events.csv`:
    data/astram_events.csv
The demo's Tab 3 (Event & Congestion Alerts) uses it. Tabs 1 & 2 work without it.
